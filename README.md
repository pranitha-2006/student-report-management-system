# student-report-management-system
This C program implements a simple console-based student management system using a binary file to store student records (ID, name, branch, phone, CGPA). On start, if the file doesn't exist, it creates it with three default students.
